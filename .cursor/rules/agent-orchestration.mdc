---
description: Agent 編排 — 僅在 /agent-plan、/agent-action 時啟用（勿 alwaysApply）
alwaysApply: false
---

# Agent Orchestration

> **啟用方式：** 使用者輸入 **`/agent-plan`** 或 **`/agent-action`** 時才依本規則與指令檔執行；一般對話不自動進入 Leader 編排模式。

完整說明：`docs/AGENT-WORKFLOW.md`。Episode domain：`docs/EPISODE-WORKFLOW.md`。

## 入口指令

| 指令 | 做什麼 | 不做什麼 |
|------|--------|----------|
| **`/agent-plan`** | Composer 寫 Plan → **Opus 4.8 + GPT 5.5 並行審核** → Approved Plan | 預設不實作、不 commit、不生圖 |
| **`/agent-action`** | 讀 Approved Plan → **拆任務、分 model 派工** → 整合 → Verify →（可選）Ship | 不重做完整 Plan（blocked 除外） |

Fable 5（`claude-fable-5-thinking-medium`）：**備選**第三意見，僅 Opus/GPT 衝突時。

## Leader

Composer 2.5（`composer-2.5-fast`）：Plan、拆任務、整合、git scope、高成本確認。

## 模型 slug（Task `model`）

| 用途 | slug |
|------|------|
| Leader | `composer-2.5-fast` |
| Plan 架構審 / L3 | `claude-opus-4-8-thinking-medium` |
| Plan 工程審 / diff review | `gpt-5.5-medium` |
| L2 / 字幕·scenes·characters | `claude-4.6-sonnet-medium-thinking` |
| explore | `grok-4.3` |
| shell / 批次 | `grok-build-0.1` |
| 備選 Plan 審 | `claude-fable-5-thinking-medium` |

## `/agent-action` 路由

- L3 → Composer 或 Opus | L2 → Sonnet 4.6 | L1 → Sonnet / Grok | L0 → Grok Build / `shell`
- 中文校對 / scenes / characters → Sonnet 或 Composer，**不要** Grok
- 可並行不同檔；**禁止**多 agent 同檔
- 子任務 prompt：Goal、Context、Constraints、Do NOT、Verification、Deliverable
- commit → Leader only；**只 stage 相關檔**

## Verify

`npm run verify:episodes` · `npm run check`

## 跳過 `/agent-plan`

單檔小改、一條命令、使用者說「直接做」→ 可只 `/agent-action`。
