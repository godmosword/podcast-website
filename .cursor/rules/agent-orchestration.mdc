---
description: Agent 編排 — 僅在 /agent-plan、/agent-action 時啟用（勿 alwaysApply）
alwaysApply: false
---

# Agent Orchestration

> **啟用方式：** 使用者輸入 **`/agent-plan`** 或 **`/agent-action`** 時才依本規則與指令檔執行；一般對話不自動進入 Leader 編排模式。

- Meta：[`docs/AGENT-WORKFLOW.md`](../../docs/AGENT-WORKFLOW.md)
- Domain：[`docs/AGENT-DOMAIN.md`](../../docs/AGENT-DOMAIN.md)
- 失敗案例簿：[`docs/AGENT-FAILURES.md`](../../docs/AGENT-FAILURES.md)（Bootstrap 必讀；委員／Task 呼叫失敗必追加；30 天連續 2+ fail → 標缺席）

## 入口指令

| 指令 | 做什麼 | 不做什麼 |
|------|--------|----------|
| **`/agent-plan`** | Leader 寫 Plan → **固定雙審**（GPT 5.6 Sol + Grok 4.5 Fast Medium；Opus 觸發制）→ Approved Plan | 預設不實作、不 commit；內容 SOP 不進委員會 |
| **`/agent-action`** | 讀 Approved Plan（或內容 SOP）→ **Task 派工** → 整合 → Verify → **可選 diff 審** →（可選）Ship；顧問一律 readonly | 不重做完整 Plan（blocked 除外） |

## 紅線

見 **AGENT-DOMAIN.md** § 紅線（Plan 違反 → CRITICAL）。

## Leader（Composer 節流）

當前 session 主模型（`composer-2.5-fast`）**只做編排**，不包辦實作與 Plan 細節：

- ✅ 讀／寫 Approved Plan **骨架**、Task 派工、整合 diff、解衝突、git scope
- ✅ 改動 **&lt;10 行**、無架構影響 → Leader 直接改
- ❌ **不要**用 Composer 寫多檔實作、長 Plan 細節、探索 codebase、跑 verify 命令 → 一律 **Task 派工**

## 模型 slug（Task `model`）

| 用途 | slug | 備註 |
|------|------|------|
| Leader（編排／整合） | `composer-2.5-fast` | **節流**：非必要不讀大檔、不長篇推理 |
| Plan 細節草稿 | `gpt-5.6-sol-medium` | Task DAG、Files、Verification、Model routing |
| Plan／diff 工程審（**預設**） | `gpt-5.6-sol-medium` | 一般 L1／L2 單審；Claude Code 用 `codex exec -m gpt-5.6` |
| Plan 架構審（**觸發制**） | `claude-opus-4-8-thinking-medium` | 跨模組／紅線／Protected／範圍不清；或 `architect` |
| Plan／diff 對抗審（**預設固定**） | `grok-4.5-fast-medium` | readonly；不審中文；**每輪 plan 必派**；slug 不可用 → 缺席勿頂替 |
| L2 多檔實作 | `claude-4.6-sonnet-medium-thinking` | **預設**；中文文案一律 Sonnet；勿用 Composer |
| L1 單檔實作 | `claude-4.6-sonnet-medium-thinking` | 路徑已知直接 Sonnet；不明才 explore |
| explore | `grok-4.3` | 只讀探索（路徑不明時） |
| shell / 批次 | `grok-build-0.1` | lint／test／verify |
| 備選 Plan 審 | `claude-fable-5-thinking-medium` | 兩路衝突時 |

遵守 Domain § Protected paths / models。

## `/agent-action` 路由

- L3 → Opus 或 Leader（Protected paths）| L2 → **Sonnet 4.6** | L1 → 已知路徑 **Sonnet**；不明才 explore → Sonnet | L0 → `shell` / Grok Build
- 可並行不同檔；**禁止**多 agent 同檔
- commit → Leader only；**只 stage 相關檔**；預設不 push

## Verify

依 **AGENT-DOMAIN.md** § 驗證矩陣。

## CRITICAL

A/B/C 格式 — 見 `review-user-choice.mdc`（若有）

## 跳過 `/agent-plan`

- 單檔小改、一條命令、使用者說「直接做」
- **內容管線**（字幕／scenes／illustrate SOP 內）→ 直做或只 `/agent-action`

## 委員會門檻（精簡）

- **預設**：**GPT 5.6 Sol + Grok 4.5 Fast Medium 雙審**（可並行）；Opus 觸發制
- **純文件／命令檔對齊**：仍列固定全表；可降級為 Leader 自審 + GPT 單審，但 **Grok 列必保留**（嘗試派工或標缺席，**禁止標跳過**）
- **Composer 可行性審（Cursor）**＝Leader 自審，不計入「至少一位非 leader 委員」
- **Grok 缺席**：GPT 仍成功即可 Approved，摘要表標 **「對抗審缺席／對抗性降級」**
- **小 diff**（約 &lt;80 行、無 Protected）：action 可跳過 diff 委員審，只跑 Verify

## 固定 Agent 執行分配表

`/agent-plan` 收尾**固定列出 #0–#5 全表**（見指令檔 §7）；**不得省略 Grok 列**。狀態只用：`完成`｜`未派`｜`缺席`｜`對抗審缺席／對抗性降級` — **禁止對 Grok 標 `跳過`**。

`/agent-action` 收尾固定列出 Leader、實作 Task、Verify、diff 審（含 Grok 列）、Ship 列；diff 審可標 `跳過`（小 diff 免審），但 plan 階段 Grok 不可跳過。

## 收尾輸出（必附）

`/agent-plan` 與 `/agent-action` 完成時，Leader **必須**附 **Agent 執行分配表**：各 agent／委員做了什麼 + `model slug` + 狀態。執行期從派工起維護；詳見 [`docs/AGENT-WORKFLOW.md`](../../docs/AGENT-WORKFLOW.md) § 收尾輸出。
