---
description: Agent 編排入口；只有 /agent-plan 或 /agent-action 時載入
alwaysApply: false
---

# Agent 編排

本規則只有使用者輸入 `/agent-plan` 或 `/agent-action` 時啟用；一般對話不自動派子 agent。

- 唯一流程來源：[`docs/AGENT-WORKFLOW.md`](../../docs/AGENT-WORKFLOW.md)
- 專案紅線與驗證：[`docs/AGENT-DOMAIN.md`](../../docs/AGENT-DOMAIN.md)
- 外部模型失敗時才讀：[`docs/AGENT-FAILURES.md`](../../docs/AGENT-FAILURES.md)

## 路由原則

- L0：直接執行命令並做最小驗證。
- L1：單一執行者；路徑不明才先 explore。
- L2：一次獨立工程審查；UI 風險追加 Opus 設計審，外部模型或安全風險追加對抗審。
- L3、Protected path、schema、sync workflow、發布或付費 API：工程、對抗、設計三審，Leader 綜合。
- 內容 SOP（字幕、scenes、illustrate）跳過完整 plan，依 Domain／Episode workflow 處理；生圖仍遵守成本紅線。

## 模型與安全

- Cursor Leader／整合：`cursor-grok-4.5-high-fast`
- Plan 細節與工程審：`gpt-5.6-luna-max-fast`
- L1/L2 實作與對抗審：`composer-2.5-fast`
- 設計審：`claude-opus-5-thinking-high`
- Claude Code 工程 CLI：`codex exec -m gpt-5.6-luna`；Grok CLI 備援：`grok-4.6`
- 禁止派工 Fable 5（`claude-fable-5-*`）；由 `.cursor/hooks/block-fable.mjs` 硬擋。
- 顧問一律 readonly；禁止多 agent 同時修改同一檔；commit／push 只由 Leader 在使用者明確要求時執行。

## 審查與完成

審查是否啟用、Approved 條件、Bootstrap、探活、Handoff、Verify 與收尾分配表，全部依 canonical workflow；不為未派出的委員建立空白固定表格。
